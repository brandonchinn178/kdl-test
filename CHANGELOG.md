# v0.2.0

* Changed command to `kdl-test run ...`
* Added more subcommands:
    * `kdl-test extract`
    * `kdl-test get`

# v0.1.3

* Fix expected output for `zero_space_before_slashdash_*` tests

# v0.1.2

* Replace `entries` with separate `args` and `props` fields
* Allow specifying specific tests to run

# v0.1.1

* Fix expected outputs for sci_notation tests
* Show input in test output

# v0.1.0

Initial release
